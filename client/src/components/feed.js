import React, {Component} from 'react';
import { Link } from 'react-router-dom'; //what is this?
import Footer from './footer'
//is a "view"

class Feed extends Component{

    showFeed() {
        //axios.get
        //push reviews into an array and loop through the array, rendering each one
        //will probably need state
    }

    render(){
        return(

        ),
    }
}
